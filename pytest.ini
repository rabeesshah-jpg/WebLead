[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
pythonpath = .
addopts = -p pytest_django.plugin
markers =
    language_gate: tests for WhatsApp language selection and gate routing
    whatsapp_menu: tests for WhatsApp menu, inactivity, and restart flows
